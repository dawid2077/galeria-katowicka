[project]
name = "fastapi-backend"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "python-dotenv",
    "sse-starlette",
    "pydantic-settings",
    "psycopg[binary]",
    "uuid-extension",
    'sqlalchemy',
    'structlog',
]
